<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:LearnXAML">

    <Style TargetType="{x:Type local:Cell}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Cell}">
                    <Image Width="200" Height="200" 
                           Source="{Binding RelativeSource={RelativeSource  AncestorType=local:Cell, Mode=FindAncestor}, Path=Image}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="{x:Type local:CustomTrafficControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CustomTrafficControl}">
                    <StackPanel>
                        <local:Cell 
                       Style="{Binding RelativeSource={RelativeSource AncestorType=local:CustomTrafficControl
                       , Mode=FindAncestor}, 
                       Path=CellStyle}"/>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        
    </Style>
    
</ResourceDictionary>
