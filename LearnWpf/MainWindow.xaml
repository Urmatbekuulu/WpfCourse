<Window x:Class="LearnXAML.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LearnXAML"
        mc:Ignorable="d"
        Name="mainWindow"
        DataContext="{Binding ElementName=mainWindow}"
        Title="MainWindow" Height="450" Width="800">
    
    <StackPanel>
        <local:GridBoxUserControl Caption="This is caption" Content="This is content"/>
        <Label Content="TestString"/>
        <TextBox Text="{Binding Path=TestString,UpdateSourceTrigger=PropertyChanged}"/>
        <Label Content="MaxLength"/>
        <TextBox Text="{Binding Path=MaxLength, UpdateSourceTrigger=PropertyChanged}"/>
        <ToggleButton Name="toggleButton" Content="Toggle Button"/>
        
    </StackPanel>
</Window>
